version: '3.8'
services:
  app:
    build: .
    ports:
      - "7259:4602"
    environment:
      - NODE_ENV=production
    volumes:
      - ./data:/app/data
  db:
    image: postgres:15-alpine
    environment:
      POSTGRES_DB: cache_runner
      POSTGRES_PASSWORD: changeme
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
